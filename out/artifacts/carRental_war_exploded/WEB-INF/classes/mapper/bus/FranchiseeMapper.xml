<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeqifu.bus.mapper.FranchiseeMapper">

    <select id="loadAllFranchisee" resultType="com.yeqifu.bus.domain.Franchisee">
        select
            id,name,phone
        from bus_franchisee
        <where>
            <if test="req.name!=null and req.name!=''">
                name LIKE concat("%",#{req.name},"%")
            </if>
        </where>
    </select>

    <insert id="addFranchisee" parameterType="com.yeqifu.bus.domain.Franchisee">
        insert into bus_franchisee (name, phone) values (#{franchisee.name}, #{franchisee.phone})
    </insert>

    <update id="updateFranchisee" parameterType="com.yeqifu.bus.domain.Franchisee">
        update bus_franchisee
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="phone != null">
                phone = #{phone,jdbcType=VARCHAR}
            </if>
        </set>
        <where>
            id = #{id,jdbcType=INTEGER}
        </where>
    </update>

    <delete id="deleteFranchisee" parameterType="java.lang.Integer">
        delete
        from bus_franchisee
        where id = #{id}
    </delete>

    <select id="selectByPrimaryKey" resultType="com.yeqifu.bus.domain.Franchisee">
        select
         id, name, phone
        from bus_franchisee
        where id = #{id}
    </select>

</mapper>